import { Container } from "react-bootstrap";

const FooterComponent = () => {
  return (
    <Container className="container-margin fixed-bottom">
      <div>FooterComponent</div>
    </Container>
  );
};

export default FooterComponent;
